list <Case> cases = [
    SELECT Id
      FROM Case];
delete cases;

// list <Account> Accounts = [ SELECT id FROM Account];
// delete Accounts;

// list <Contact> Contacts = [ SELECT id FROM Contact];
// delete Contacts;